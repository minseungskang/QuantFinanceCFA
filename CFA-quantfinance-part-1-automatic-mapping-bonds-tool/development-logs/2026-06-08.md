# Development Log - 2026-06-08

## Completed

- Prepared the demo-focused stabilization pass without changing the scoring model.
- Kept the one-click `Start Demo.command` launcher for macOS demos.
- Kept the README demo instructions for VS Code and browser access.
- Kept the scrollable ranking table and sticky table header for presentation use.
- Kept the `Data Confidence` wording change to `Data Completeness`.
- Kept header tooltips for Rating, Total Score, and Completeness.
- Kept the Data Note card explaining that sample completeness is not source verification.
- Removed the Windows shortcut `.lnk` file from the project.
- Added `*.lnk` to `.gitignore` so shortcut files are not committed again.

## In Progress

- Sync the stabilized demo files from the Felix repository to the minseung group repository.

## Todo

- Begin the next scoring-model pass only after this demo stabilization is verified and pushed.
- In the next pass, address YTM / Real Yield duplicate scoring and inflation double counting.

## Decisions

- Do not change scoring logic in this pass.
- Prioritize a stable demo path for the presentation.
- Treat `Data Completeness` as model field availability, not market-source verification.
- Keep the minseung repository copy as a synchronized subfolder of the Felix project.

## Risks / Notes

- The current 103-country dataset still contains sample values and should not be presented as fully market-sourced data.
- The app is still a local preview, not a packaged desktop app.

## Verification

- Ran `node scripts/check-files.mjs` successfully.
- Ran `node scripts/test-scoring.mjs` successfully.
- Started the local preview server at `http://127.0.0.1:4173`.
- Confirmed the local preview returned HTTP 200 and contains `Sovereign Bond Screening Tool`.
- Verified the page renders 103 rows in the local browser preview.
- Verified Search returns Singapore as a single visible result.
- Verified Region filter shows 27 Europe records from a clean page state.
- Verified Country A-Z sorting starts with Algeria from a clean page state.
- Verified View opens a detail row and Edit opens/closes the edit modal.
- Verified the Export CSV control is present; direct download cannot be completed in the Codex in-app browser because downloads are unsupported there.
