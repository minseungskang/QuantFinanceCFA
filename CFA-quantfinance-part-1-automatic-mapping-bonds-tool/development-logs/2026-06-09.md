# 2026-06-09 Development Log

## Completed

- Polished the table header info tooltip styling to match the intended dark floating help text.
- Replaced native browser `title` tooltips with custom `data-tooltip` content for consistent demo behavior.
- Added subtle table scrollbar styling for the wide scoring table.
- Removed the A/B/C grade filter controls so the table shows all visible countries by default.
- Reworked the table header `i` help icons into floating dark tooltips that match the requested screenshot style more closely.
- Added `Bond Type` support for grouping records into `Local Currency` and `Eurobond` categories.
- Added a top-level `Bond Type` filter and a table `Bond Type` column with an explanatory `i` tooltip.
- Restored the table `Rating` display to the original credit rating values such as `AAA`, `AA+`, and `A+`; removed the model-generated rating band from the visible UI.
- Changed the table `YTM` column from read-only text to a compact manual input that saves locally and recalculates rankings on change.
- Confirmed the screenshot sample countries `China`, `South Korea`, `India`, `Indonesia`, and `Thailand` use `5.61%` YTM and `A+` credit rating in the sample dataset.

## In Progress

- Demo UI validation in the local browser after adding the bond type filter.

## Todo

- Confirm whether the sample country order/data should match the teammate screenshot exactly.
- If approved, sync the bond-type grouping UI to the minseung group repository after local verification.

## Decisions

- Keep this as a UI-only pass; do not change scoring weights or sample data in this step.
- Use Search, Region, Sort, Pin, and Hide as the main table controls. Avoid A/B/C grade filter buttons on the main screen.
- Use floating `i` help bubbles for compact table explanations instead of browser-native title tooltips.
- Treat old records without a bond type as `Local Currency` so existing browser-local data remains compatible.
- Do not generate or display a separate A/B/C-style rating band from rank percentile; use the source `Credit Rating` field for the Rating column.
- Let users manually update `YTM` directly from the table for faster demo edits.

## Risks / Notes

- The screenshot's first rows show a different sample data/order than the current local branch. That may require a separate data sync decision.
