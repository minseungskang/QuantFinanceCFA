# 2026-06-09 Development Log

## Completed

- Polished the table header info tooltip styling to match the intended dark floating help text.
- Replaced native browser `title` tooltips with custom `data-tooltip` content for consistent demo behavior.
- Added subtle table scrollbar styling for the wide scoring table.
- Removed the A/B/C grade filter controls so the table shows all visible countries by default.
- Reworked the table header `i` help icons into floating dark tooltips that match the requested screenshot style more closely.
- Added `Bond Type` support for grouping records into `Local Currency` and `Eurobond` categories.
- Added a top-level `Bond Type` filter and a table `Bond Type` column with an explanatory `i` tooltip.
- Restored the table `Rating` display to the original credit rating values such as `AAA`, `AA+`, and `A+`; removed the model-generated rating band from the visible UI.
- Changed the table `YTM` column from read-only text to a compact manual input that saves locally and recalculates rankings on change.
- Confirmed the screenshot sample countries `China`, `South Korea`, `India`, `Indonesia`, and `Thailand` use `5.61%` YTM and `A+` credit rating in the sample dataset.
- Used the June 2026 103-country Excel workbook as the country-universe classification source.
- Cleaned the demo universe to 88 comparable records: 48 `Local Currency` records and 40 `Eurobond` records.
- Set the default `Bond Type` filter to `Local Currency`, so the first demo view shows the 48 active local-currency 10Y sovereign bond records.
- Excluded the 6 default/restructuring countries and 9 countries without a 10Y sovereign bond from the visible demo ranking.
- Added local-data pruning so old browser-local sample records that are no longer in the current eligible universe are removed on reload.
- Verified the local browser preview shows 48 default `Local Currency` records, 40 `Eurobond` records, and 88 records in `All bond types`.
- Verified the excluded default/restructuring and no-10Y countries do not appear in the visible `All bond types` ranking.
- Ran the project scaffold check and scoring tests successfully after the Excel-based universe update.
- Replaced the bond-type dropdown presentation with a segmented control for direct `Regular`, `Eurobond`, and `All` switching.
- Verified the segmented control in the browser preview: `Regular` shows 48 records, `Eurobond` shows 40 records, and `All` shows 88 records.
- Added automatic bond-type classification repair for older browser-local records so cached data is reclassified into `Regular` and `Eurobond` without requiring a manual reset.

## In Progress

- None for this pass.

## Todo

- Confirm whether the next scoring-model pass should adjust YTM / real-yield overlap first or benchmark scaling first.

## Decisions

- Keep this as a UI-only pass; do not change scoring weights or sample data in this step.
- Use Search, Region, Sort, Pin, and Hide as the main table controls. Avoid A/B/C grade filter buttons on the main screen.
- Use floating `i` help bubbles for compact table explanations instead of browser-native title tooltips.
- Use a segmented control for bond-type switching so regular local-currency bonds and Eurobonds are separated without opening a dropdown.
- Treat old records without a bond type as `Local Currency` so existing browser-local data remains compatible.
- Do not generate or display a separate A/B/C-style rating band from rank percentile; use the source `Credit Rating` field for the Rating column.
- Let users manually update `YTM` directly from the table for faster demo edits.
- Treat the Excel workbook as a country eligibility and bond-type classification source, not as a complete numeric market-data source.
- Keep default/restructuring and no-10Y countries outside the visible ranking for the demo.

## Risks / Notes

- The Excel workbook does not contain YTM, price, CDS, reserve, or other full scoring inputs; those numeric demo values still come from the app sample dataset until a complete market-data file is provided.
